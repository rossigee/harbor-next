version: '3'

vars:
  # Paths
  COMPOSE_FILE: devenv/docker-compose.yml
  VERSIONS_ENV: versions.env
  COMPOSE_LOCAL_FILE: deploy/compose/docker-compose.yaml
  COMPOSE_LOCAL_ENV: deploy/compose/example/local/.env.example
  TOKEN_KEY_FILE: devenv/token_service_key.pem
  TOKEN_KEY_FILE_LOCAL: deploy/compose/config/token_service_key.pem

  # Database (only vars used in task commands)
  POSTGRES_USER: postgres
  POSTGRES_DB: registry

  # Slot-based port allocation: SLOT=0 gives default ports, SLOT=1 offsets +100, etc.
  SLOT: '{{.SLOT | default "0"}}'

  PORT_CORE:         '{{ add 8080  (mul (atoi .SLOT) 100) }}'
  PORT_PORTAL:       '{{ add 4200  (mul (atoi .SLOT) 100) }}'
  PORT_POSTGRES:     '{{ add 5432  (mul (atoi .SLOT) 100) }}'
  PORT_REDIS:        '{{ add 6379  (mul (atoi .SLOT) 100) }}'
  PORT_REGISTRY:     '{{ add 50000 (mul (atoi .SLOT) 100) }}'
  PORT_REGISTRYCTL:  '{{ add 8085  (mul (atoi .SLOT) 100) }}'
  PORT_TRIVY:        '{{ add 8081  (mul (atoi .SLOT) 100) }}'
  PORT_DEBUG_CORE:   '{{ add 2345  (mul (atoi .SLOT) 100) }}'
  PORT_DEBUG_JOB:    '{{ add 2346  (mul (atoi .SLOT) 100) }}'
  PORT_DEBUG_REGCTL: '{{ add 2347  (mul (atoi .SLOT) 100) }}'

  PROJECT_PREFIX: 'harbor-{{.SLOT}}'

  # Base docker compose command with env-file for build args (GO_VERSION, BUN_VERSION, etc.)
  COMPOSE_CMD: 'docker compose -f {{.COMPOSE_FILE}} --env-file {{.VERSIONS_ENV}}'

  # Docker Compose profile strings (reused across multiple tasks)
  ALL_PROFILES: "--profile portal --profile core --profile jobservice --profile registryctl --profile trivy"
  APP_PROFILES: "--profile portal --profile core --profile jobservice --profile registryctl"
  BACKEND_PROFILES: "--profile core --profile jobservice --profile registryctl"

env:
  COMPOSE_PROJECT_NAME:   '{{.PROJECT_PREFIX}}'
  PROJECT_PREFIX:         '{{.PROJECT_PREFIX}}'
  HOST_GOMODCACHE:
    sh: go env GOMODCACHE
  HOST_GOCACHE:
    sh: go env GOCACHE
  PORT_CORE:              '{{.PORT_CORE}}'
  PORT_PORTAL:            '{{.PORT_PORTAL}}'
  PORT_POSTGRES:          '{{.PORT_POSTGRES}}'
  PORT_REDIS:             '{{.PORT_REDIS}}'
  PORT_REGISTRY:          '{{.PORT_REGISTRY}}'
  PORT_REGISTRYCTL:       '{{.PORT_REGISTRYCTL}}'
  PORT_TRIVY:             '{{.PORT_TRIVY}}'
  PORT_DEBUG_CORE:        '{{.PORT_DEBUG_CORE}}'
  PORT_DEBUG_JOBSERVICE:  '{{.PORT_DEBUG_JOB}}'
  PORT_DEBUG_REGISTRYCTL: '{{.PORT_DEBUG_REGCTL}}'
  DEV_UID:
    sh: id -u
  DEV_GID:
    sh: id -g
  # Rootless Podman maps the host user to container root, so UID 0 is the
  # writable identity for bind-mounted worktrees there.
  DEV_CONTAINER_USER:
    sh: |
      if docker version 2>/dev/null | grep -q "Podman Engine" && \
        docker info 2>/dev/null | grep -q "rootless: true"; then
        echo "0:0"
      else
        echo "harbor"
      fi

tasks:
  # ---------------------------------------------------------------------------
  # Tool Installation
  # ---------------------------------------------------------------------------
  tools:install:
    internal: true
    status:
      - test -x {{.GOPATH_BIN}}/{{.TOOL_NAME}}
    cmd: go install {{.TOOL_PKG}}

  setup:
    desc: Install Go development tools (air, dlv, govulncheck)
    cmds:
      - for:
          - { name: air, pkg: 'github.com/air-verse/air@{{.AIR_VERSION}}' }
          - { name: dlv, pkg: 'github.com/go-delve/delve/cmd/dlv@{{.DELVE_VERSION}}' }
          - { name: govulncheck, pkg: 'golang.org/x/vuln/cmd/govulncheck@latest' }
        task: tools:install
        vars:
          TOOL_NAME: '{{.ITEM.name}}'
          TOOL_PKG: '{{.ITEM.pkg}}'
      - echo "All development tools installed"

  require-docker:
    internal: true
    run: once
    silent: true
    preconditions:
      - sh: command -v docker
        msg: "Docker is not installed. Please install Docker first."
      - sh: docker info > /dev/null 2>&1
        msg: "Docker daemon is not running. Please start Docker first."

  require-ports:
    internal: true
    run: once
    silent: true
    cmds:
      - |
        failed=0
        for pair in \
          "{{.PORT_POSTGRES}}:PostgreSQL" \
          "{{.PORT_REDIS}}:Redis" \
          "{{.PORT_CORE}}:Core" \
          "{{.PORT_PORTAL}}:Portal" \
          "{{.PORT_REGISTRY}}:Registry" \
          "{{.PORT_REGISTRYCTL}}:RegistryCtl" \
          "{{.PORT_TRIVY}}:Trivy"; do
          port="${pair%%:*}"
          name="${pair##*:}"
          if ss -tlnH "sport = :$port" 2>/dev/null | grep -q .; then
            pid=$(ss -tlnpH "sport = :$port" 2>/dev/null | sed -n 's/.*pid=\([0-9]*\).*/\1/p' | head -1)
            echo "ERROR: Port $port ($name) is already in use${pid:+ by PID $pid}"
            failed=1
          fi
        done
        if [ "$failed" -eq 1 ]; then
          echo ""
          echo "Free the ports above, or use a different slot:  task dev:up SLOT=1"
          echo "SLOT=1 offsets all ports by +100 (PostgreSQL→{{add .PORT_POSTGRES 100}}, Redis→{{add .PORT_REDIS 100}}, …)"
          exit 1
        fi

  gen:private-key:
    desc: Generate RSA 4096 key for token signing (skipped if exists)
    status:
      - test -f {{.TOKEN_KEY_FILE}}
    cmds:
      - openssl genpkey -algorithm RSA -outform PEM -pkeyopt rsa_keygen_bits:4096 | openssl rsa -traditional -out {{.TOKEN_KEY_FILE}}
      - chmod 644 {{.TOKEN_KEY_FILE}}
      - echo "Generated {{.TOKEN_KEY_FILE}}"

  # ---------------------------------------------------------------------------
  # Info
  # ---------------------------------------------------------------------------
  info:
    desc: Print SLOT, port assignments, and service URLs
    silent: true
    cmd: |
      echo ""
      echo "Harbor Dev Environment (SLOT={{.SLOT}})"
      echo "================================"
      echo "  Portal:       http://localhost:{{.PORT_PORTAL}}"
      echo "  Core API:     http://localhost:{{.PORT_CORE}}/api/v2.0"
      echo "  PostgreSQL:   localhost:{{.PORT_POSTGRES}}"
      echo "  Redis:        localhost:{{.PORT_REDIS}}"
      echo "  Registry:     localhost:{{.PORT_REGISTRY}}"
      echo "  RegistryCtl:  localhost:{{.PORT_REGISTRYCTL}}"
      echo "  Trivy:        localhost:{{.PORT_TRIVY}}"
      echo "  Prefix:       {{.PROJECT_PREFIX}}"
      echo ""

  # ---------------------------------------------------------------------------
  # Full Development Environment (backend containers + native Portal)
  # ---------------------------------------------------------------------------
  _codegen:
    internal: true
    deps:
      - :build:gen-apis
      - _frontend-codegen
      - _warm-caches

  _warm-caches:
    internal: true
    cmds:
      - mkdir -p src/portal/.angular
      - cmd: cd src && go mod download
        silent: true

  _frontend-codegen:
    internal: true
    cmds:
      - task: frontend:install
      - task: :build:gen-apis:frontend

  up:
    desc: "Start backend containers and native Portal with hot reload (SKIP_TRIVY=true to exclude Trivy)"
    deps: [require-docker, require-ports, gen:private-key]
    vars:
      TRIVY_PROFILE: '{{if ne .SKIP_TRIVY "true"}}--profile trivy{{end}}'
    cmds:
      - defer: { task: down, vars: { SKIP_TRIVY: "{{.SKIP_TRIVY}}" } }
      - task: _codegen
      - task: info
      - '{{.COMPOSE_CMD}} {{.BACKEND_PROFILES}} {{.TRIVY_PROFILE}} up -d --build'
      - task: _wait-core
      - task: _frontend-serve

  down:
    desc: Stop and remove all dev environment containers
    vars:
      TRIVY_PROFILE: '{{if ne .SKIP_TRIVY "true"}}--profile trivy{{end}}'
    cmds:
      - task: _frontend-stop
      - '{{.COMPOSE_CMD}} {{.BACKEND_PROFILES}} {{.TRIVY_PROFILE}} down'

  _frontend-stop:
    internal: true
    silent: true
    cmds:
      - |
        expected="./node_modules/@angular/cli/bin/ng serve --host 0.0.0.0 --port {{.PORT_PORTAL}} --hmr"
        pid=""

        if command -v lsof >/dev/null 2>&1; then
          pid=$(lsof -nP -t -iTCP:{{.PORT_PORTAL}} -sTCP:LISTEN 2>/dev/null | head -1 || true)
        fi
        if [ -z "$pid" ] && command -v ss >/dev/null 2>&1; then
          pid=$(ss -tlnpH "sport = :{{.PORT_PORTAL}}" 2>/dev/null | sed -n 's/.*pid=\([0-9]*\).*/\1/p' | head -1 || true)
        fi

        if [ -z "$pid" ]; then
          exit 0
        fi

        args=$(ps -p "$pid" -o command= 2>/dev/null || ps -p "$pid" -o args= 2>/dev/null || true)
        cwd=""
        if [ -e "/proc/$pid/cwd" ]; then
          cwd=$(readlink "/proc/$pid/cwd" 2>/dev/null || true)
        elif command -v lsof >/dev/null 2>&1; then
          cwd=$(lsof -a -p "$pid" -d cwd -Fn 2>/dev/null | sed -n 's/^n//p' | head -1)
        fi
        case "$args" in
          *"$expected"*)
            if [ -z "$cwd" ] || [ "$cwd" = "$PWD/src/portal" ]; then
              kill "$pid" 2>/dev/null || true
            fi
            ;;
        esac
        exit 0

  # ---------------------------------------------------------------------------
  # Infrastructure (Docker Compose)
  # ---------------------------------------------------------------------------
  infra:up:
    desc: Start PostgreSQL, Redis, and Registry (waits for DB readiness)
    deps: [require-docker, require-ports]
    cmds:
      - '{{.COMPOSE_CMD}} up -d'
      - |
        echo "Waiting for PostgreSQL..."
        timeout=60 elapsed=0
        until {{.COMPOSE_CMD}} exec -T postgresql pg_isready -U postgres > /dev/null 2>&1; do
          [ $elapsed -ge $timeout ] && echo "PostgreSQL failed to start" && exit 1
          sleep 2; elapsed=$((elapsed + 2))
        done
      - echo "Infrastructure ready"
      - '{{.COMPOSE_CMD}} ps'

  infra:down:
    desc: Stop PostgreSQL, Redis, and Registry containers
    cmd: '{{.COMPOSE_CMD}} down'

  infra:remove:
    desc: Stop all containers and delete all named volumes
    cmd: '{{.COMPOSE_CMD}} {{.ALL_PROFILES}} down -v'

  # ---------------------------------------------------------------------------
  # Backend Services (Containerized)
  # ---------------------------------------------------------------------------
  backend:up:
    desc: Build and start Core, JobService, RegistryCtl with hot reload
    deps: [require-docker, require-ports, gen:private-key]
    cmds:
      - task: :build:gen-apis
      - '{{.COMPOSE_CMD}} --profile core --profile jobservice --profile registryctl up -d --build'
      - |
        echo ""
        echo "API: http://localhost:{{.PORT_CORE}}/api/v2.0"
        echo ""

  backend:down:
    desc: Stop and remove Core, JobService, RegistryCtl (keeps infra running)
    cmds:
      - '{{.COMPOSE_CMD}} stop core jobservice registryctl'
      - '{{.COMPOSE_CMD}} rm -f core jobservice registryctl'

  backend:logs:
    desc: Tail logs from Core, JobService, and RegistryCtl
    cmd: '{{.COMPOSE_CMD}} --profile core --profile jobservice --profile registryctl logs -f'

  backend:logs:*:
    desc: "Tail logs for a specific backend service (core, jobservice, registryctl)"
    cmd: '{{.COMPOSE_CMD}} logs -f {{index .MATCH 0}}'

  backend:restart:
    desc: Restart Core, JobService, and RegistryCtl containers
    cmd: '{{.COMPOSE_CMD}} restart core jobservice registryctl'

  _wait-core:
    internal: true
    silent: true
    cmds:
      - |
        echo "Waiting for Core at http://localhost:{{.PORT_CORE}}..."
        timeout=120
        elapsed=0
        until curl -fsS "http://localhost:{{.PORT_CORE}}/api/v2.0/ping" >/dev/null 2>&1; do
          if [ "$elapsed" -ge "$timeout" ]; then
            echo "Core did not become ready within ${timeout}s"
            exit 1
          fi
          sleep 2
          elapsed=$((elapsed + 2))
        done
        echo "Core ready"

  # ---------------------------------------------------------------------------
  # Frontend (Native - alternative to containerized portal)
  # ---------------------------------------------------------------------------
  frontend:install:
    internal: true
    dir: src/portal
    preconditions:
      - sh: command -v bun
        msg: "bun is required for native frontend dev. Install: https://bun.sh"
    sources:
      - package.json
      - bun.lock
    generates:
      - node_modules/.bun-integrity
    cmd: bun install --ignore-scripts

  frontend:native:
    desc: Serve Portal with ng serve on host (alternative to containerized portal)
    silent: true
    deps: [frontend:install]
    cmds:
      - task: :build:gen-apis:frontend
      - task: _frontend-serve

  _frontend-serve:
    internal: true
    dir: src/portal
    env:
      HARBOR_PROXY_TARGET: 'http://localhost:{{.PORT_CORE}}'
    cmd: exec node --max_old_space_size=2048 ./node_modules/@angular/cli/bin/ng serve --host 0.0.0.0 --port {{.PORT_PORTAL}} --hmr

  frontend:build:
    desc: Build Portal for production
    dir: src/portal
    cmd: npm run build

  frontend:test:
    desc: Run frontend tests in watch mode
    dir: src/portal
    cmd: npm run test -- --watch

  frontend:test:once:
    desc: Run frontend tests once
    dir: src/portal
    cmd: npm run test -- --watch=false

  # ---------------------------------------------------------------------------
  # Database Operations
  # ---------------------------------------------------------------------------
  db:migrate:
    desc: Run pending database migrations against local PostgreSQL
    dir: src
    env:
      POSTGRESQL_HOST: localhost
      POSTGRESQL_PORT: '{{.PORT_POSTGRES}}'
      POSTGRESQL_USERNAME: '{{.POSTGRES_USER}}'
      POSTGRESQL_PASSWORD: root123
      POSTGRESQL_DATABASE: '{{.POSTGRES_DB}}'
      POSTGRESQL_SSLMODE: disable
      POSTGRES_MIGRATION_SCRIPTS_PATH: ../make/migrations/postgresql/
    cmd: go run ./cmd/standalone-db-migrator/main.go

  db:reset:
    desc: Drop database, recreate it, and apply all migrations from scratch
    prompt: "This will delete all data in {{.POSTGRES_DB}}. Continue?"
    cmds:
      - '{{.COMPOSE_CMD}} exec -T postgresql dropdb -U {{.POSTGRES_USER}} {{.POSTGRES_DB}} --if-exists'
      - '{{.COMPOSE_CMD}} exec -T postgresql createdb -U {{.POSTGRES_USER}} {{.POSTGRES_DB}}'
      - task: db:migrate
      - echo "Database reset complete"

  db:shell:
    desc: Open interactive psql session on the dev database
    cmd: '{{.COMPOSE_CMD}} exec postgresql psql -U {{.POSTGRES_USER}} -d {{.POSTGRES_DB}}'

  # ---------------------------------------------------------------------------
  # Release Test (deploy/compose)
  # ---------------------------------------------------------------------------
  gen:private-key:local:
    internal: true
    status: ['test -f {{.TOKEN_KEY_FILE_LOCAL}}']
    cmds:
      - mkdir -p $(dirname {{.TOKEN_KEY_FILE_LOCAL}})
      - openssl genpkey -algorithm RSA -outform PEM -pkeyopt rsa_keygen_bits:4096 | openssl rsa -traditional -out {{.TOKEN_KEY_FILE_LOCAL}}
      - chmod 644 {{.TOKEN_KEY_FILE_LOCAL}}

  release:up:
    desc: "Start release images locally (IMAGE_REPO= for remote, empty for local)"
    deps: [require-docker, gen:private-key:local]
    vars:
      TAG: '{{.TAG | default .VERSION}}'
      REPO: '{{.IMAGE_REPO | default ""}}'
    env:
      HARBOR_TAG: '{{.TAG}}'
      IMAGE_REPO: '{{.REPO}}'
    cmds:
      - defer: { task: release:down, vars: { TAG: "{{.TAG}}", IMAGE_REPO: "{{.REPO}}" } }
      - echo "Release {{.TAG}} {{if .REPO}}({{.REPO}}){{else}}(local){{end}} → http://localhost admin/Harbor12345"
      - docker compose -f {{.COMPOSE_LOCAL_FILE}} --env-file {{.COMPOSE_LOCAL_ENV}} up

  release:down:
    desc: Stop release containers
    vars:
      TAG: '{{.TAG | default .VERSION}}'
      REPO: '{{.IMAGE_REPO | default ""}}'
    env:
      HARBOR_TAG: '{{.TAG}}'
      IMAGE_REPO: '{{.REPO}}'
    cmd: docker compose -f {{.COMPOSE_LOCAL_FILE}} --env-file {{.COMPOSE_LOCAL_ENV}} down

  release:down:clean:
    desc: Stop release containers and remove all volumes
    vars:
      TAG: '{{.TAG | default .VERSION}}'
      REPO: '{{.IMAGE_REPO | default ""}}'
    env:
      HARBOR_TAG: '{{.TAG}}'
      IMAGE_REPO: '{{.REPO}}'
    cmd: docker compose -f {{.COMPOSE_LOCAL_FILE}} --env-file {{.COMPOSE_LOCAL_ENV}} down -v

  # ---------------------------------------------------------------------------
  # Utilities
  # ---------------------------------------------------------------------------
  clean:
    desc: Remove all containers, volumes, node_modules, and temp files
    cmds:
      - '{{.COMPOSE_CMD}} {{.ALL_PROFILES}} down -v'
      - task: _frontend-stop
      - rm -rf tmp/
      - rm -rf src/portal/node_modules
      - rm -f build-errors.log
      - rm -f {{.TOKEN_KEY_FILE}}
      - rm -f {{.TOKEN_KEY_FILE_LOCAL}}
      - rmdir $(dirname {{.TOKEN_KEY_FILE_LOCAL}}) 2>/dev/null || true

  status:
    desc: Show container status for all services
    cmd: '{{.COMPOSE_CMD}} {{.ALL_PROFILES}} ps'

  logs:
    desc: Tail logs from all containers
    cmd: '{{.COMPOSE_CMD}} {{.ALL_PROFILES}} logs -f'
